# 应用配置
APP_NAME=Mideas 应用
APP_VERSION=1.0.0
DEBUG=True

# 服务器配置
HOST=0.0.0.0
PORT=18888

# 数据库配置（使用 SQLite3）
DATABASE_ECHO=False

# 日志配置
LOG_DIR=/work/logs/MIdeasServer

# ==================== GPT Researcher 配置 ====================

# OpenAI API 配置（用于 LLM）
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_API_BASE=https://api.openai.com/v1  # 可选，自定义 API 端点
OPENAI_MODEL=gpt-4  # 可选，默认 gpt-4

# 搜索引擎配置（选择其中一个）
# 1. Tavily（推荐，专为 AI 研究设计）
TAVILY_API_KEY=your_tavily_api_key_here

# 2. Google Search（需要 Google Custom Search API）
# GOOGLE_API_KEY=your_google_api_key_here
# GOOGLE_CX=your_custom_search_engine_id_here

# 3. Bing Search
# BING_API_KEY=your_bing_api_key_here

# 4. Serper（Google Search API 替代）
# SERPER_API_KEY=your_serper_api_key_here

# GPT Researcher 其他配置
RETRIEVER=tavily  # 搜索引擎类型：tavily, google, bing, serper, duckduckgo

# Embedding 配置（用于文档相似度计算）
# 如果使用本地 Embedding 服务，配置以下选项：
# EMBEDDING_PROVIDER=custom
# EMBEDDING_API_URL=http://localhost:18888/mideasserver/embedding
# EMBEDDING_MODEL=text-embedding-local

# 如果使用 OpenAI Embedding（默认），确保 OPENAI_API_KEY 已配置
# GPT Researcher 会自动使用 text-embedding-3-small 模型

MAX_SEARCH_RESULTS=5  # 每次搜索的最大结果数
BROWSE_CHUNK_MAX_LENGTH=8192  # 网页内容分块大小
SUMMARY_TOKEN_LIMIT=700  # 摘要 token 限制
